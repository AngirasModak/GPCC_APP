# GPCC Portal V2 – Phase 1
Includes professional navigation, dashboard, Supabase-powered Income CRUD, edit and soft-delete.
Copy over the existing repository, keep .env.local, run npm install, npm run dev, then commit and push.