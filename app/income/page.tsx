"use client";import {useEffect,useState} from "react";import {supabase} from "../../lib/supabase";
type Row={id:string,date:string,contributor:string,flat_no:string,amount:number,mode:string,reference:string,status:string};const initial={date:new Date().toISOString().slice(0,10),contributor:"",flat_no:"",amount:"",mode:"Cash",reference:"",status:"Cleared"};
export default function Income(){const[rows,setRows]=useState<Row[]>([]);const[form,setForm]=useState<any>(initial);const[open,setOpen]=useState(false);const[editing,setEditing]=useState<string|null>(null);const[msg,setMsg]=useState("");const load=async()=>{const{data,error}=await supabase.from("income").select("*").is("deleted_at",null).order("date",{ascending:false});if(error)setMsg(error.message);else setRows(data||[])};useEffect(()=>{load()},[]);const save=async()=>{if(!form.contributor||!form.amount){setMsg("Contributor and amount are required.");return}const payload={...form,amount:Number(form.amount)};let error:any;if(editing)({error}=await supabase.from("income").update(payload).eq("id",editing));else({error}=await supabase.from("income").insert(payload));if(error){setMsg(error.message);return}setOpen(false);setEditing(null);setForm(initial);setMsg("");load()};const edit=(r:Row)=>{setEditing(r.id);setForm({...r,amount:String(r.amount)});setOpen(true)};const del=async(id:string)=>{if(!confirm("Delete this income entry?"))return;const{error}=await supabase.from("income").update({deleted_at:new Date().toISOString()}).eq("id",id);if(error)setMsg(error.message);else load()};return <><div className="pageHead"><div><h1>Income & Puja Subscription</h1><p className="muted">Cash, cheque and online collections</p></div><button className="btn" onClick={()=>{setEditing(null);setForm(initial);setOpen(true)}}>+ Add Income</button></div>{msg&&<div className="card" style={{marginBottom:14,color:"#b42318"}}>{msg}</div>}<div className="card tableWrap"><table className="table"><thead><tr><th>Date</th><th>Contributor</th><th>Flat</th><th>Mode</th><th>Reference</th><th>Amount</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td>{r.date}</td><td>{r.contributor}</td><td>{r.flat_no||"-"}</td><td>{r.mode}</td><td>{r.reference||"-"}</td><td>₹{Number(r.amount).toLocaleString("en-IN")}</td><td><span className="status">{r.status}</span></td><td className="actions"><button className="btn secondary" onClick={()=>edit(r)}>Edit</button><button className="btn danger" onClick={()=>del(r.id)}>Delete</button></td></tr>):<tr><td colSpan={8} className="empty">No income entries yet.</td></tr>}</tbody></table></div>{open&&<div className="modalBg"><div className="modal"><div className="pageHead"><h2>{editing?"Edit Income":"Add Income"}</h2><button className="btn secondary" onClick={()=>setOpen(false)}>Close</button></div><div className="formGrid">{[["date","Date","date"],["contributor","Contributor Name","text"],["flat_no","Flat / House No.","text"],["amount","Amount","number"],["reference","Cheque / UTR / Receipt No.","text"]].map(([k,l,t])=><label key={k}>{l}<input className="input" type={t} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label>Payment Mode<select className="input" value={form.mode} onChange={e=>setForm({...form,mode:e.target.value})}><option>Cash</option><option>Cheque</option><option>Online</option></select></label><label>Status<select className="input" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>Cleared</option><option>Pending</option><option>Cancelled</option></select></label></div><div style={{marginTop:20}}><button className="btn" onClick={save}>{editing?"Update Entry":"Save Entry"}</button></div></div></div>}</>}