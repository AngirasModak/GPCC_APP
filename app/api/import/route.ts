import {NextRequest,NextResponse} from "next/server";import * as XLSX from "xlsx";export async function POST(req:NextRequest){const f=(await req.formData()).get("file");if(!(f instanceof File))return NextResponse.json({error:"Excel file required"},{status:400});if(f.size>10*1024*1024)return NextResponse.json({error:"Maximum 10 MB"},{status:413});const wb=XLSX.read(Buffer.from(await f.arrayBuffer()),{type:"buffer"});const rows=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:""});return NextResponse.json({rows:rows.length,preview:rows.slice(0,10)});}